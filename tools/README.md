# Tools

Some tools generated by AI entirely. See the top comments of them.

## top_included

### top_included.sh

Count the number of header files included by `#include`.

## sk2vx

### enums.py

```
$ tools/sk2vx/enums.py [DIR1] [DIR2] [...]
```

Convert enum names and values. Based on `rename_config.json` file in the
project's root directory.
